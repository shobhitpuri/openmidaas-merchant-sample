# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /fulfill                    controllers.Application.fulfill
POST    /checkout                   controllers.Application.checkout
GET     /listen                     controllers.Application.listen

# MIDaaS endpoints
GET     /r/:id                      controllers.MIDaaSRequest.process(id)
POST    /postback                   controllers.MIDaaSResponse.process




# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
